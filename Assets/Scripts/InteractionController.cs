using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* TODO: will be responsible for controller all player behavior (tool/weapon switching, firing weapons,
 * interacting with in game buttons/objects)
 */
public class InteractionController : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
